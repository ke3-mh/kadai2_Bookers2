<h1>New book</h1>
  <%= form_with model: @post_image do |f| %>
    <h4>Title</h4>
      <%= f.text_field :name %>
    <h4>Option</h4>
      <%= f.text_area :body %>
    <%= f.submit 'Create Book' %>
  <% end %>